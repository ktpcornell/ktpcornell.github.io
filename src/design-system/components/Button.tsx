/**
 * KTP Button — thin re-export from the ShadCN primitive layer.
 * The canonical implementation lives in src/components/ui/button.tsx.
 */
export { Button, ktpButtonVariants, buttonVariants } from '@/components/ui/button'
export type { ButtonProps } from '@/components/ui/button'
