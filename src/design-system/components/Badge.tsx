/**
 * KTP Badge — thin re-export from the ShadCN primitive layer.
 * The canonical implementation lives in src/components/ui/badge.tsx.
 */
export { Badge, ktpBadgeVariants, badgeVariants } from '@/components/ui/badge'
export type { BadgeProps } from '@/components/ui/badge'
